03.03 将光标移动到指定的字符上 (fx)

fx: 在当前行上查找右边第一个字符x，并将光标移动过去；
nfx: 在当前行数查找右边第n个字符x，并将光标移动过去；
Fx: 在当前行上查找左边第一个字符x，并将光标移动过去；
nFx: 在当前行数查找左边第n个字符x，并将光标移动过去；

tx: 同fx，只是光标移动到字符x的左边字符，光标少移动一个字符距离；
ntx: 同nfx，只是光标移动到字符x的左边字符，光标少移动一个字符距离；
Tx: 同Fx，只是光标移动到字符x的右边字符，光标少移动一个字符距离；
nTx: 同nFx，只是光标移动到字符x的右边字符，光标少移动一个字符距离；

;: 重复上述命令；
,: 反向重复上述命令，即执行上述命令的大小写转换后形式；

VIM应该有一个寄存器用来存储上述8中光标移动指令的最后一个，而使用;和,则可以获取寄存器中的指令并执行相应操作；
;和,所执行的操作不会更新寄存器，因此多次使用,并不会出现反复的情况，而是一直沿着使用;和,之前的那条指令进行反向移动；
;和,会忽视上述操作中的n，只进行无n情况下的移动；
可以理解为nfx的实现是执行n个fx，然后寄存器只记录最后一个操作，也就是fx，并不会记录n的信息；


03.04 将光标移动到匹配的括号上 (%)

%: 将光标跳转到与当前光标下的括号（包括(),[],{}等）相匹配的另一个括号上。

如果当前光标并不在括号的位置，则会向右寻找第一个括号，将其作为光标下的括号，将光标跳转到与其匹配的括号上。
