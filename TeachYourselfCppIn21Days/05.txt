第5章 使用表达式、语句和运算符

语句中的空白通常被忽略。  {}语句块或者符合语句。
对unsigned整型变量执行减法运算时，如果结果为负，则会出现奇怪的结果，变量溢出。
使用括号来实现自己需要的优先级运算。    ANSI bool类型占用1字节，非零值为true，零值为false。

if(expression)
  statement1;
else
  statement2;
使用大括号来进行if和else的正确匹配。

运算符优先级与C语言相同。
